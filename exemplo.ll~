
int main()
{	
	int i = 1;
	int j = 20;
	
	i+= 20 + 1 + j;
	//valor esperado: 42
	cout(i);

	i-= 20 + 1 + j;
	//valor esperado: 1
	cout(i);

	i-= 2;
	//valor esperado: -1
	cout(i);

	i*= -1;
	//valor esperado: 1
	cout(i);

	i/= 1/2;
	//valor esperado: 2
	cout(i);

	1 + 2 + (int) 3.0 + 8;

	return 0;
}
