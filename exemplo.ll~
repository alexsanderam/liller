int main()
{	
	/*Aqui é um comentário
	Este arquivo é de exemplo da Linguagem Liller
	e do Compilador prescot-liller.*/

	int r = 1, j = 10;
	bool x = true;
	
	x = x + j;

	cout(j + r);
	cout (x);

	return 0;
}
