int main(void)
{
	/*int d;
	int size;
	
	cout("\nEnter the value to d: ");
	cin(d);

	cout("\nEnter the size of matrix: ");
	cin(size);

	float matrix[size][size];*/

	string a;

	cin(a);

	return 0;
}
